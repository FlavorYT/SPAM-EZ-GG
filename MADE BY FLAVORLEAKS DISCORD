loopRunning = true
    
    while loopRunning do
    game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer("! .gg/7W944krM || EZ RAID LOL || SERVER RAIDED.", "All")
    wait()
end
