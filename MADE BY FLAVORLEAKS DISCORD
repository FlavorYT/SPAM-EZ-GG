loopRunning = true
    
    while loopRunning do
    game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer("! .gg/Jpxrg3cJ || EZ RAID LOL || SERVER RAIDED.", "All")
    wait()
end
